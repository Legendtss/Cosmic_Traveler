# FitTrack Pro - Production Dependencies

# Core Flask
Flask==3.0.0
Flask-Cors==4.0.0
gunicorn==22.0.0
Werkzeug==3.1.3

# AI/LLM
google-generativeai==0.8.6

# HTTP
requests==2.32.3

# Environment
python-dotenv==1.2.1

# Database
# SQLite is built-in, but PostgreSQL requires psycopg2
# psycopg2 compiles from source, which is more compatible with Render/Alpine Linux
# Set DATABASE_URL environment variable to enable PostgreSQL support
psycopg2==2.9.9
